# Environment And Secrets

Secrets and network authorization now live in [External Perimeter](external-perimeter.md).

Use `$NAME` in `trial_runtime.agent.command` or `trial_runtime.agent.env` to bind values supplied through `runtime.secrets` and launch-time `--env` / `--env-file` inputs.
