{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-lab.local/schemas/trial_input_v1.json",
  "title": "Trial Input v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "ids", "case", "runtime", "design"],
  "properties": {
    "schema_version": { "const": "trial_input_v1" },
    "ids": {
      "type": "object",
      "additionalProperties": false,
      "required": ["run_id", "trial_id", "variant_id", "case_id", "repl_idx"],
      "properties": {
        "run_id": { "type": "string", "minLength": 1 },
        "trial_id": { "type": "string", "minLength": 1 },
        "variant_id": { "type": "string", "minLength": 1 },
        "case_id": { "type": "string", "minLength": 1 },
        "repl_idx": { "type": "integer", "minimum": 0 }
      }
    },
    "case": {
      "type": "object",
      "additionalProperties": true,
      "description": "Case payload. Schema is package-specific."
    },
    "task_schema": { "type": "string" },
    "workload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["agent_harness", "trainer", "custom"]
        }
      }
    },
    "design": {
      "type": "object",
      "additionalProperties": false,
      "required": ["sanitization_profile", "integration_level"],
      "properties": {
        "sanitization_profile": {
          "type": "string",
          "enum": ["replay_strict", "hermetic_functional", "perf_benchmark"]
        },
        "integration_level": {
          "type": "string",
          "enum": ["cli_basic", "cli_events", "otel", "control_checkpoint", "control_full"]
        }
      }
    },
    "runtime": {
      "type": "object",
      "additionalProperties": false,
      "required": ["network_mode", "allowed_hosts", "case_image", "workdir", "time_limit_ms"],
      "properties": {
        "network_mode": {
          "type": "string",
          "enum": ["none", "full", "allowlist_enforced"]
        },
        "allowed_hosts": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "case_image": { "type": "string", "minLength": 1 },
        "workdir": { "type": "string", "minLength": 1 },
        "time_limit_ms": { "type": "integer", "minimum": 1 }
      }
    },
    "ext": { "type": "object", "additionalProperties": true }
  }
}
