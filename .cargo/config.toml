# `cargo bucephalus-full` builds the local-only analytics binary (DuckDB-backed
# `bucephalus views` / `bucephalus query`). The shipped product is the core CLI without it.
[alias]
bucephalus-full = "build --release --features duckdb_engine --bin bucephalus"
lab-full = "build --release --features duckdb_engine --bin lab"
