# `cargo lab-full` builds the local-only analytics binary (DuckDB-backed
# `lab views` / `lab query`). The shipped product is the core CLI without it.
[alias]
lab-full = "build --release --features duckdb_engine --bin lab"
